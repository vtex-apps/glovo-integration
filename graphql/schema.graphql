type Query {
  getGlovoIntegrationSettings: GlovoIntegrationSettings
    @cacheControl(scope: PUBLIC, maxAge: SHORT)
}

type Mutation {
  saveGlovoIntegrationSettings(settings: GlovoIntegrationSettingsInput): Boolean
}
